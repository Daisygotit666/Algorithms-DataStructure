package datastructure.linked_list;

/**
 * 在计算机科学中，链表是数据元素的线性集合，其每个元素都指向下一个元素，元素存储上并不连续
 * 
 * 分类：
 * 1. 单向链表：每个元素只知道下一个元素是谁
 * 2. 双向链表：每个元素知道其上一个元素和下一个元素
 * 3. 循环链表： 通常的链表尾结点tail指向的是null，而循环链表的tail指向的是头节点head
 * 
 * 链表内还有一种特殊的节点称为哨兵节点，也叫做哑元节点，它不存储数据，通常用作头尾，用来简化边界判断
 * 随机访问的时间复杂度 O(1)
 */
public class LinkedList {
  
}
